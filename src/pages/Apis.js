import React from 'react';
import apis from './apis.png';

const style = {
  height: '100vh',
  width: 'calc(100vw - 70px)',
};

const Apis = () => (
  <img style={style} src={apis} />
);

export default Apis;
